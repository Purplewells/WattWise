namespace WattWise.Services
{
    public class CreditUpdateLogService
    {
        // Logs and manages credit update events (e.g., API-based credit updates, token-based updates).
        // business logic for handling credit updates
        // This includes logging credit update events, managing credit update history, and providing an interface for querying credit update logs.
        // For example, if a credit update is received via API, it can be logged in the system.
        // This service can also handle the logic for querying credit update logs based on various criteria, such as date range, meter serial number, and update type.
        // LogCreditUpdate(string meterSerial, decimal amount, string updateType)

        // LogCreditUpdate(string meterSerial, decimal amount, string updateMethod)
        // GetCreditUpdateLogs()
        // GetCreditUpdateLogsForMeter(string meterSerial)
    }
}
