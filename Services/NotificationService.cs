namespace WattWise.Services
{
    public class NotificationService
    {
        //Manages the sending of different types of notifications (e.g., low credit, payment confirmation, meter configuration updates).

        //Methods:

        //SendPaymentConfirmationAsync(string userEmail, string paymentDetails)

        //SendMeterConfigurationUpdateNotificationAsync(string userEmail, string configurationDetails)

        //SendLowCreditNotificationAsync(string userPhoneNumber, decimal balance)

        //SendLowBatteryNotificationAsync(string userPhoneNumber, decimal batteryLevel)
    }
}
