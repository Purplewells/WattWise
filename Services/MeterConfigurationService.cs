using Org.BouncyCastle.Pqc.Crypto.Lms;
using System.Reflection.Metadata;

namespace WattWise.Services
{
    //summary>
    // Handles remote configuration of meters via API(e.g., setting credit limits, tariffs, and other configuration parameters).
    /// <summary>
    public class MeterConfigurationService
    {
        //ConfigureMeterAsync(string meterSerial, decimal creditLimit, string tariff)

        //GetMeterConfigurationAsync(string meterSerial)
    }
}
