using WattWise.Models;

namespace WattWise.Services
{
    public class UserService
    {
        // Handles user management, including registration, role assignments, and access control.
        // Methods:
        
        // RegisterUserAsync(string username, string password, string email)
        // AssignRoleToUserAsync(string username, string role)
        // CreateUserAsync(ApplicationUser user, string password)
        // AssignRoleToUserAsync(string userId, string roleName)
        // GetUserByIdAsync(string userId)
        // GetUsersByRoleAsync(string roleName)
    }
}
